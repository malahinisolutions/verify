!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    = favicon_link_tag 'https://idv.amltoken.com/assets/logo.png', :type => 'image/png'
    %meta{:content => "width=device-width, initial-scale=1, maximum-scale=1", :name => "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "AML Bitcoin"
    %meta{:content => "#{content_for?(:description) ? yield(:description) : "Www"}", :name => "description"}/
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = javascript_include_tag params[:controller], 'data-turbolinks-track' => true if Rails.application.assets.find_asset(params[:controller].to_s).present?
    = csrf_meta_tags

    


    /[if lt IE 9]
      <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
    %script
      var loading_image = new Image();
      loading_image.src = '/assets/loading_page.gif';
  %body.login-page
    #preloader
      #status.with_bg
    = gflash
    %section.login_bg
      %header
        .container
          = link_to image_tag("small-logo.png", class: "img-responsive"), root_path
      .container
        .login-box
          = yield

      =render 'layouts/footer'
